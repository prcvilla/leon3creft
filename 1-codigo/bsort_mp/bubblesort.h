#ifndef BUBBLESORT_H
#define BUBBLESORT_H
#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

void printarr(uint8_t *);

int checkarr(uint8_t *);

void fillArray(uint8_t *);

void bubblesort(uint8_t* arr);

#endif
